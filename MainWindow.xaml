<Window x:Class="FencesApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FencesApp" Height="600" Width="900"
        WindowStyle="SingleBorderWindow" Background="White"
        Loaded="Window_Loaded">
    <Window.Resources>
        <!-- Estilo para los botones del menú -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Width" Value="160"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1ABC9C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#16A085"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#ECF0F1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Menú lateral (agregamos x:Name para identificarlo en el código) -->
        <StackPanel x:Name="MenuPanel" Grid.Column="0" Background="#2C3E50" Width="200">
            <TextBlock Text="App" Foreground="White" FontSize="20" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="20"/>
            <Button Content="{DynamicResource HomeButton}" Tag="Home" Click="NavigateToPage"/>
            <Button Content="{DynamicResource WindowButton}" Tag="Fences" Click="NavigateToPage"/>
            <Button Content="{DynamicResource SettingsButton}" Tag="Settings" Click="NavigateToPage"/>
            <Button Content="{DynamicResource AboutButton}" Tag="About" Click="NavigateToPage"/>
        </StackPanel>

        <!-- Contenedor de contenido -->
        <Grid Grid.Column="1">
           

            <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" Background="White"/>
        </Grid>
    </Grid>
</Window>
